#include<stdio.h>
#include<math.h>
#include<stdlib.h>

int main(){
	int n, i, j, noc = 0;
	scanf("%d", &n);
	float r1, r2, revtar;
	scanf("%f%f", &r1, &r2);
	scanf("%f", &revtar);
	
	printf("%d", cnt);
}
